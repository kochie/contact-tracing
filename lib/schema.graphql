type CheckIn {
    location_id: String
    user_id: String
    checkin_datetime: AWSDateTime
}

type Query {
    get_user_location_history(user_id: String!, from: AWSDateTime, until: AWSDateTime): [CheckIn]
    get_location_attendees(location_id: String!, from: AWSDateTime, until: AWSDateTime): [CheckIn]
}

type Mutation {
    check_in(location_id: String!, user_id: String!): CheckIn
}

type Schema {
    query: Query
    mutation: Mutation
}